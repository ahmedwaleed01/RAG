## Install Requirments

```bash
pip install -r requirements.txt
```

## Run FastApi Server

```bash
uvicorn main:app --reload
```

## Build Docker

```bash
docker compose -f docker-compose.yml up -d --build
```
